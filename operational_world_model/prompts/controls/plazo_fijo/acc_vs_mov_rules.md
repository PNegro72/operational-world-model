# Accounting vs Movement Rules

Accounting to Movement focus.

Main date field:
- ACCOUNTING_ACC_MOV_DATE_CREATED

Focus on accounting entries that should have associated movement.
Use `ACCOUNTING_*` fields as accounting reference.
Do not mix with movement date unless the selected branch requires it.
